import React, { memo } from "react"
import {} from "@vizality/components/settings"

export default memo(({ getSetting, updateSetting, toggleSetting }) => {
  return (
    <>
      <div>You dont get plugins</div>
    </>
  );
});
